#include <bits/stdc++.h>
#define FOR(x,a,b) for(int x=a;x<b;++x)
using namespace std;

signed main()
{
    int h,w,n,m; cin >> h >> w >> n >> m;
    int img[h][w],k[n][m];
    FOR(i,0,h) FOR(j,0,w) cin >> img[i][j];
    FOR(i,0,n) FOR(j,0,m) cin >> k[i][j];
    FOR(i,0,h-1)
    {
        FOR(i,0,w-1)
        {
            
        }
    }

}