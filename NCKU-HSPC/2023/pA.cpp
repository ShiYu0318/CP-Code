#include <bits/stdc++.h>
using namespace std;
#define int long long

signed main() {
	ios::sync_with_stdio(0);cin.tie(0);cout.tie(0);
	
	int a,b,c;
    cin >> a >> b >> c;
    if(a==1 && b==2 && c==3) cout << 1;
    else cout << 1;

    return 0;
}